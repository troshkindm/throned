---
name: upstream-merge
description: Merge changes from the upstream Throne project into this fork — surveying what is new, resolving the conflicts this repository reliably produces, and verifying the result. Use when asked to catch up with upstream, sync the fork, or when an upstream merge conflicted.
---

Pointer. The procedure lives in `.agents/skills/upstream-merge/SKILL.md` — read that file and follow it.

There is one canonical copy and it lives under `.agents/`, because that is where Codex looks. Claude Code only discovers skills under `.claude/skills/`, hence this stub. Edit `.agents/`; touch this file only when the description changes.
