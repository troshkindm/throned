---
name: build
description: Configure, build and test Throned on Windows or Linux, and verify that the build tree is actually tracking header dependencies. Use before running anything, after changing CMake files, or whenever an edit appears to have no effect on the binary.
---

Pointer. The procedure lives in `.agents/skills/build/SKILL.md` — read that file and follow it.

There is one canonical copy and it lives under `.agents/`, because that is where Codex looks. Claude Code only discovers skills under `.claude/skills/`, hence this stub. Edit `.agents/`; touch this file only when the description changes.
