---
name: add-outbound
description: Add support for a new outbound protocol to Throned — config model, editor widget, factory registration, subscription parsing and tests. Use when asked to support a new proxy protocol or to extend an existing one's fields.
---

Pointer. The procedure lives in `.agents/skills/add-outbound/SKILL.md` — read that file and follow it.

There is one canonical copy and it lives under `.agents/`, because that is where Codex looks. Claude Code only discovers skills under `.claude/skills/`, hence this stub. Edit `.agents/`; touch this file only when the description changes.
