---
name: ui-snapshot
description: Render and compare Throned's UI screenshots — the pinned gate versus the look-at-it gallery, how to read a diff, and when a baseline may be updated. Use when a change touches widgets, layout, themes or stylesheets, or when a snapshot run failed.
---

Pointer. The procedure lives in `.agents/skills/ui-snapshot/SKILL.md` — read that file and follow it.

There is one canonical copy and it lives under `.agents/`, because that is where Codex looks. Claude Code only discovers skills under `.claude/skills/`, hence this stub. Edit `.agents/`; touch this file only when the description changes.
