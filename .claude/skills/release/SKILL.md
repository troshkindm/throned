---
name: release
description: Cut a Throned release — tag format, what the tag workflow builds and publishes, and how to write the localized release notes the in-app updater reads. Use when asked to prepare, tag or publish a release, or to write release notes.
---

Pointer. The procedure lives in `.agents/skills/release/SKILL.md` — read that file and follow it.

There is one canonical copy and it lives under `.agents/`, because that is where Codex looks. Claude Code only discovers skills under `.claude/skills/`, hence this stub. Edit `.agents/`; touch this file only when the description changes.
