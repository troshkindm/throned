# Commits that only moved whitespace. Blame skips them once Git is told to:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add a full 40-character SHA on its own line, with the subject as a comment
# above it. Only ever add a commit that changes formatting and nothing else.

# style: format first-party sources with clang-format
29e49b97a0a004ba71302c438ea10ffc9f5befcb
